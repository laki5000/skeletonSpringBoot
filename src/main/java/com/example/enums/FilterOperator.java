package com.example.enums;

/** Enum for filter operator. */
public enum FilterOperator {
    EQUALS,
    CONTAINS,
    STARTS_WITH,
    ENDS_WITH,
    NOT_EQUALS,
    GREATER_THAN,
    LESS_THAN,
    BETWEEN
}
